/* In principle, we could have our own separate scanner for FOL that
 * handles things differently than the PL scanner. However, for
 * now, they're unified.
 */

#pragma once

#include "LogicScanner.h"

namespace FOL {
    using namespace Logic;
}
